import { Meta } from '@storybook/react';
import { {{pascalCase name}} } from '.';

export default {
  title: '{{pascalCase name}}',
  component: {{pascalCase name}},
  parameters: {
    backgrounds: {
      default: 'primary',
    },
  },
} as Meta;

export const Default = () => <{{pascalCase name}} />;
